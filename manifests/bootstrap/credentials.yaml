apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: common
  namespace: {{ ns }}
  labels:
    cluster.open-cluster-management.io/type: hostinventory
    cluster.open-cluster-management.io/credentials: ""
stringData:
  baseDomain: "{{ domain }}"
  pullSecret: "{{ pullsecret }}"
  ssh-publickey: "{{ pubkey }}"